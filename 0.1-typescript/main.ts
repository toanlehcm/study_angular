let a: string;
let b: number;
let c: boolean;
let d: any;
let e: string[] = ['a', 'b', 'c']; // number[], boolean[], any[]
let f: null;
let g: undefined;
let h: void;

enum Color {
    Red,
    Green,
    Blue
}